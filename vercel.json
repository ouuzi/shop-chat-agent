{
  "functions": {
    "app/**/*.js": {
      "runtime": "nodejs18.x"
    }
  },
  "buildCommand": "prisma generate && npm run build",
  "installCommand": "npm install && prisma generate",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/app/$1"
    }
  ]
}